# Render API URL (no trailing slash). Set the same in Vercel → Environment Variables.
VITE_API_URL=https://agentos-wazq.onrender.com/

# mock | rest — use rest when VITE_API_URL points at Render
VITE_API_MODE=rest

# Demo login (server): demo@agentos.ai / agentos123
