/** Very light grid overlay on cream page sections (scrolls with content). */
export default function MarketingGridBackground() {
  return (
    <div
      className="at-grid-bg pointer-events-none fixed inset-0 -z-10 h-full w-full"
      aria-hidden
    />
  );
}
